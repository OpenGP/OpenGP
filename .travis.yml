language: cpp
compiler: clang
os: osx
script:
  - mkdir build && cd build && cmake -DCMAKE_BUILD_TYPE=Debug .. && cmake --build .
before_install:
  - brew update
  - brew install eigen
notifications:
  email: true
